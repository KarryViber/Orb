# Skills

Place Claude Code agent-format skill files (.md) in this directory.
They will be automatically indexed and injected into the agent's system prompt.

Each skill file should include `name:` and `description:` frontmatter fields
so the agent can discover and reference them.
