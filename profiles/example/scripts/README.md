# Scripts

Place utility scripts here for the agent to execute during task processing.
Scripts can be in any language (Python, Node.js, Bash, etc.)

The agent can reference this directory via the `## Scripts` section
injected into the system prompt.
